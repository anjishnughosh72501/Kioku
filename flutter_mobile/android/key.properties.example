# Example key.properties for production Android release signing
# Copy to flutter_mobile/android/key.properties and fill with your production keystore credentials.
# NEVER commit key.properties or .jks/.keystore files to Git!

storePassword=your_keystore_password
keyPassword=your_key_alias_password
keyAlias=kioku_release_key
storeFile=../upload-keystore.jks
